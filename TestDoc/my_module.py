# 在您的Python文件夹中创建一个名为“my_module.py”的新文件，并在其中编写以下代码：

# 在模块“my_module”中定义一个名为“greeting”的变量，其值为“Hello, ”
greeting = "Hello, "

# 在模块“my_module”中定义一个名为“greet”的函数，它接受一个参数“name”，并打印一个问候语


def greet(name):
    # 在控制台中打印出包含“greeting”变量和“name”参数的字符串
    print(f"{greeting}{name}!")

